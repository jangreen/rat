let rf = rf & loc
let co = co & loc
let fr = rf^-1;co
let po-loc = po & loc

let com = (co | fr | rf)
let uniproc = (po-loc | com)^+ & id

assume loc^-1 <= loc
assume loc*   <= loc      // Necessary to show   fr & int   <= po
assume po+    <= po       // Necessary to show   com+ & int <= po

assume uniproc <= 0

let int = po | po^-1 | id

// assert rf & int <= po
// assert co & int <= po
// assert fr & int <= po
assert com+ & int <= po
