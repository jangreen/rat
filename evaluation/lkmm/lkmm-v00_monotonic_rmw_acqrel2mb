
// ========= Common definitions =========
let int = po | po^-1 | id
let rf = [W];rf;[R]
let rfi = rf & int
let rfe = rf & ext
let rf = rfi | rfe
let M = R | W

let rmw = [R];rmw;[W] & po
let RMW = domain(rmw) | range(rmw)

// ========= Common assumptions =========
assume Marked & Plain <= 0
assume W & R <= 0
assume po & id <= 0
assume po+ <= po
assume rfi <= po
assume rmw^-1;rmw <= id
assume rmw;rmw^-1 <= id

// ========= Actual comparison =========

// Ensure that R and W parts of rmw are adjacent in po
let po = po | po;[R];rmw | po;[W];rmw^-1

let AcquireOrig = Acquire
let ReleaseOrig = Release

let Acquire = AcquireOrig | domain(rmw)
let Release = ReleaseOrig | range(rmw)
include ./evaluation/lkmm/lkmm-v00.cat as old

let Acquire = AcquireOrig
let Release = ReleaseOrig
let Mb = Mb | domain(rmw) | range(rmw)
include ./evaluation/lkmm/lkmm-v00.cat as new

// assert old.hb <= new.hb                    // TIMEOUT
// The following fails. See lkmm-v04 version of this test where it succeeds.
assert old.ppo          <= new.ppo             // FALSE
assert old.cumul-fence  <= new.cumul-fence     // FALSE

// assert new.hb <= old.hb                    // TIMEOUT
// assert new.ppo         <= old.ppo          // FALSE
// assert new.cumul-fence <= old.cumul-fence  // FALSE