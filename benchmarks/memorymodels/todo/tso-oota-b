let rfe = rf-base & ext
let rfi = rf-base & int & loc
let rf = rfi | rfe
let dep = [R];(dep-base & po)

let oota = (dep | rf)^+ & id

assume rf & id <= 0
assume po^+ <= po
assume rfi <= po
assume ([R];po | [W];po;[W] | rfe)^+ & id <= 0

assert dep;rfe;rfi & id <= 0
assert oota <= 0
//assert rfi;rfi;rfe & id <= 0