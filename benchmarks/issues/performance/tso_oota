let int = po | po^-1
let rfi = rf-base & int & loc
let rf = rfi | rfe
let fr = rf^-1;co

let po-tso = ([R];po | [W];po;[W])
let tso = (po-tso | fr | rfe)^+ & id
let oota = ([R];po | rf)^+ & id

assume tso <= 0
assert oota <= 0
