{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "|= Proof",
            "type": "shell",
            "command": "${workspaceFolder}/build/CatInfer ${file}",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}