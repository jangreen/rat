let rf = [W];rf-base;[R]
let co = [W];co-base;[W]
let fr = rf^-1;co

let eco1 = (rf | co | fr)+
let eco2 = rf | co ; rf? | fr ; rf?


assume W & R <= 0         // W ∩ R = ∅
assume co-base+ <= co-base
assume rf;rf^-1 <= id

assert eco2 <= eco1
assert eco1 <= eco2